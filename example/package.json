{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "node .",
    "refresh": "yarn cache clean && yarn install --force --no-lockfile",
    "test": "ava"
  },
  "dependencies": {
    "ava": "3.3.0",
    "make-cli-tool": "file:../make-cli-tool.tgz"
  }
}
